<resources>
    <!-- 进度条风格 -->
    <style name="style_progressbar">
        <item name="android:indeterminateDrawable">@drawable/gui_progress_draw</item>
        <item name="android:indeterminateBehavior">repeat</item>
        <item name="android:indeterminateDuration">3500</item>
        <!--     <item name="android:minWidth">36dp</item>
                <item name="android:maxWidth">36dp</item>
                <item name="android:minHeight">36dp</item>
                <item name="android:maxHeight">36dp</item>-->
    </style>

    <!-- webview进度条风格 -->
    <style name="style_horizontal_progressbar" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/gui_progressbar_horizontal</item>
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">10dip</item>
    </style>

    <!-- 弹出框风格 -->
    <style name="style_dialog" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!-- activity不变暗 -->
    </style>

    <!-- 弹出框弹出，退出动画 -->
    <style name="style_anim_dialogWindow" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/gui_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/gui_dialog_exit</item>
    </style>

    <!-- 通用的EditText风格，其他不一样的再已功能名命名 -->
    <style name="style_edittext_common" mce_bogus="1" parent="@android:style/Widget.EditText">
        <!--<item name="android:textAppearance">@style/Gui_TextStyle_BlueMid_Normal</item>-->
        <!--<item name="android:textColorHint">@color/gray_mid</item>-->
    </style>
</resources>
